{% if recommendations.performed? and recommendations.products_count > 0  %}    <div data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit=4&intent=related" li-if="recommendations.performed? and recommendations.products_count > 0 " class="padding-global product-recommendations">
    <div id="empfohlen" class="anchor-link"></div><div class="container-large"><div class="padding-section-medium swiper-container"><div class="section_headline"><h2 class="heading-style-h2">
{{ section.settings.text_headline }}
</h2>
</div><div><div class="swiper is-product-slider"><ul role="list" class="product-recommendations_grid w-list-unstyled">{% for product in recommendations.products %}    <li li-for="product in recommendations.products" class="swiper-slide is-product-slider">
                                    
                            {% render 'product_item', product: product %}

                            
                        </li>
{% endfor %}</ul></div></div></div></div>    </div>
{% endif %} {% schema %} {"tag":"section","name":"recommended","class":"section_product-recommended","settings":[{"id":"text_headline","type":"text","label":"Headline","default":"Das k\u00f6nnte Dir auch gefallen"}],"presets":[{"name":"recommended","category":"Liquify","blocks":[]}]} {% endschema %}